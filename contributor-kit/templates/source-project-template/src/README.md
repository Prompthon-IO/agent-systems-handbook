# Source Folder

Add only the runtime files that are directly needed by the example.

If the project is still a starter, this folder can stay small while the project
index explains the intended direction.
